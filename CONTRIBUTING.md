# Contributing

Open an issue before adding a source adapter. Document its official access method, terms, rate limits, provenance fields, and failure behavior. Keep pull requests focused and run `npm run lint && npm test` before submission.
